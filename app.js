/* ================================================
   Vinyl Collection Player v2 — Pure client-side SPA
   IndexedDB storage, Discogs API, YouTube player

   Refactored: source split across src/ modules.
   See index.html for load order.
   ================================================ */
