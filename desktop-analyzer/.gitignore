# Rust build artifacts — hundreds of files, hundreds of MB, all regenerable
# with `cargo build`.
/target

# The yt-dlp sidecar binary is fetched at setup time, not vendored.
/binaries

# The oracle is a dev tool: its virtualenv, audio and results never belong in git.
oracle/.venv/
oracle/work/

# Native static libraries, built by scripts/build-native.sh.
native/
